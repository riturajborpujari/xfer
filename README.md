# XFer
A simple file upload server for the local network

![terminal-screen-showing-running-xfer-application](./images/screenshot.png)

## Features
- Provides QR code to scan to start upload
- Provides minimal Web UI to upload file

## Usage
Run `npm install` to install the packages required

Then run `node index.js` to start the server
